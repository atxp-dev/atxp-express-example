{
  "version": 2,
  "installCommand": "npm run install-all",
  "buildCommand": "npm run build",
  "outputDirectory": "frontend/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}